export enum EFramework {
  React = 'react',
  Vue = 'vue',
  Solid = 'solid',
  Vanilla = 'vanilla',
}

export enum EProjectType {
  App = 'app',
  Library = 'library',
}

export enum EAppBundler {
  Webpack = 'webpack',
  Vite = 'vite',
}

export enum ELibraryBundler {
  Rollup = 'rollup',
  Esbuild = 'esbuild',
}

export enum EManager {
  Npm = 'npm',
  Yarn = 'yarn',
  Pnpm = 'pnpm',
  Bun = 'bun',
}
